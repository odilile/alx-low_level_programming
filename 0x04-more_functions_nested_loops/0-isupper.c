#include "main.h"

/**
 * _isupper - checks for uppercase character.
 * @c: first parameter.
 * Return: 1 if upper 0 otherwise.
 */

int _isupper(int c)
{
if (c >= 'A' && c <= '2')
{
return (1);
}

else
{
return (0);
}

}
